{% extends "layout.twig" %}

{% block content %}
    {% for user in users %}
        * {{ user.name }}
        * {{ user.email }}
        * {{ user.password }}
    {% else %}
        No users have been found.
    {% endfor %}

{% endblock %}